# 首轮架构优化

- [x] 拒绝非 SHA1 / 6 位 / 30 秒的 TOTP URI，并提供明确的多语言错误提示。
- [x] 设置写入失败向上传递，恢复界面状态并显示错误。
- [x] 实现真实的开发文件监听，合并连续变更并避免并发构建。
- [x] 补充参数解析、存储失败和 watch 验证；运行测试、类型检查和生产构建。

## 验证记录

- `bun run test`：45 项通过，新增 URI 参数和存储异常测试。
- `bun run type-check`：通过。
- `bun run build`：通过。
- 隔离副本 watch 验证：首次构建、源码变化、public 变化、根配置变化、构建失败后恢复、生成文件不循环触发、SIGTERM 退出均通过。
- 本轮未进行真实 Chrome 扩展界面点击验收；可加载 `dist/` 检查二维码错误提示和设置交互。

## 双设备固定 ID 测试

- [x] 生成测试公钥写入 Manifest，核算固定 ID。
- [x] 构建并打包同一份测试扩展，记录双设备安装步骤。

## 导入数量修复

- [x] 普通备份预读数量；加密备份解密后确认数量；仅加密备份显示密码框。
- [x] 错误反馈、解析测试、构建并更新固定 ID 测试包。

- 导入修复验证：48 项测试、类型检查和生产构建通过；固定 ID 测试包已更新。未进行真实 Chrome 界面点击验收。

## 发布包

- [x] 移除测试固定公钥，构建并验证 Chrome 商店上传 ZIP。

## 官网内容更新

- [x] 更新中英文功能、密码保护、同步与导入说明，以及页面元信息。
- [x] 构建验证通过（中英文首页与指南共四个入口）；版本与结构化 JSON 校验通过，本地预览返回 HTTP 200。
- [x] 已部署至现有 Cloudflare 官网；线上版本接口、中英文首页与指南的新内容均已验证。
